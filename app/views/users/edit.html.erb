<h1>Editing User</h1>

<%= render 'form', user: @user %>

<% if is_admin? || is_current_user?(@user)  %>
<%= link_to 'Show', user_path(@user,:html) %> 
<% end %>
<% if is_admin? %>
| <%= link_to 'Users', users_path(:html) %>
<% end %>